export function isGreaterEnd(index: number, listEnd: number): boolean {
	return index + 1 >= listEnd;
}

export function isLessStart(index: number, listStart: number): boolean {
	return index <= listStart;
}
